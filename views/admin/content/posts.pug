.alert.alert-primary
  //- button.close(type='button', data-dismiss='alert') ×
  strong Posts  
  |  are entries displayed on your sites blog.

.row
  .col

    table.table.table-hover.table-striped
      thead
        tr
          th Title
          th Created
          th Updated
          th Status
      tbody
        each p, i in posts.data
          //- -
          //-   p.updated = moment(p.updated)
          //- - p.updated = moment(p.updated).format('LLL')
          tr
            td
              a(href=`/admin/content/posts/edit/` + p._id) #{p.title}
            td= p.created
            td= p.updated
            td= p.status
